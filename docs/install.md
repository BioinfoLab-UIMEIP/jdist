# Installation

## Dependencies
- C++17 compiler
- OpenMP
- OpenCL headers + runtime (GPU optional; CPU OpenCL fallback supported)

## Build
```bash
make
```

This produces `./jdist`.

## Minimal test
```bash
make run
```
